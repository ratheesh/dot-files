# Einstellungen fuer Tmux

# basic_setings {

# default shell
# set-option -g default-shell /bin/zsh
set -g default-shell /usr/local/bin/zsh
# set-option -g default-shell /usr/local/bin/fish

# support 256 color and truecolor
set -g default-terminal "screen-256color"
set -ga terminal-overrides ",xterm-256color:Tc:smcup@:rmcup@"

# Make default prefix to Ctrl-a
set -g prefix C-a
unbind C-b
bind C-a send-prefix

# }

# keybindings {

# Reload source file
bind-key r source-file ~/.tmux.conf \; display-message "Configuration reloaded"
bind-key Space last-window
bind-key C-Space previous-window
bind-key -n F11 previous-window
bind-key -n F12 next-window

# Remap the vertical and horizontal split to '|' and '-' respectively
unbind %
bind-key | split-window -h -c "#{pane_current_path}"
bind-key - split-window -v -c "#{pane_current_path}"

# make sure that the new window open in the same path as current active window
bind-key c new-window -c "#{pane_current_path}"

# Make the current window the first window
bind T swap-window -t 1

# bind copy mode to ESC key
#unbind [
#bind-key Escape copy-mode

# }

# common settings {

set -g status-keys emacs
set-window-option -g mode-keys vi
# bind y run-shell "tmux show-buffer | xclip -sel clip -i" \; display-message "Copied tmux buffer to system clipboard"

# for tmux > v2.1
set-option -g mouse on

# start window indexing at one instead of zero
set -g base-index 1
set -g pane-base-index 1

# set the history limit to 10k lines
set -g history-limit 10000

# shorten command delay
set -sg escape-time 0
set -g aggressive-resize on

# tabs
set-option -g set-titles on
set-option -g set-titles-string '[#S:#I #H](#{pane_current_path})'
set-window-option -g xterm-keys on # to make ctrl-arrow, etc. work

# No title renames please
set -g automatic-rename off
set -g allow-rename off

set -g status on
set -g status-interval 1

# }

# misc settings {

# clock-mode
set-window-option -g clock-mode-colour cyan
set-window-option -g clock-mode-style 12

# Load screensaver type of config for idle.
# Make sure to install required program before
# set -g lock-after-time 300
# set -g lock-command "/usr/local/bin/asciiquarium"

# status bar
#set-option -g status-position top

# }

# windows_eyecandy {

# set-option -g status-attr dim

# set colors for panel borders
set -g pane-border-fg magenta
set -g pane-active-border-fg green
set -g pane-active-border-bg default

# colorize messages in the command line
set -g message-bg colour16
set -g message-fg colour26

set -g status-fg colour16
set -g status-bg colour233
set -g status-left-length 50

set -g monitor-activity on
set -g visual-activity off
set -gw window-status-activity-attr blink

set-window-option -g window-status-fg #000000
set-window-option -g window-status-bg #ffffff

set -g window-status-separator ""
set -g status-left '#[fg=#000000, bg=#8b814c] ⧠ #S #[default]'
set -g window-status-format "#{?window_activity_flag,#[fg=colour16 bg=colour244],\
#[fg=colour231 bg=colour239,noblink]} #I #{?window_activity_flag,\
#[bg=colour236 fg=colour226],#[bg=colour236 fg=colour231]} #W "
set -g window-status-current-format "#[fg=#ffffff bg=#8b0000 bold] #I \
#[fg=#000000 bg=colour178 nobold] #W #[default]"

set -g status-right-length 150
set -g status-right '#[fg=#ffffff,bg=#8f3f71] #($HOME/.tmux/tmux.netspeed.sh) \
#[fg=#ffffff,bg=#076678] %a #[fg=colour16,bg=#6e8b3d] %_d-%b \
#[fg=#ffffff,bg=#2f4f4f] %I:%M%p #[fg=#cd853f bg=colour236]#{cpu_percentage}\
#[fg=colour15]|#{online_status} #[default]'

# windows_eyecandy }

#plugins {

# List of plugins
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-logging'
set -g @plugin 'tmux-plugins/tmux-online-status'
set -g @plugin 'tmux-plugins/tmux-copycat'
set -g @plugin 'tmux-plugins/tmux-yank'
set -g @plugin 'tmux-plugins/tmux-open'
set -g @plugin 'tmux-plugins/tmux-cpu'

# Other examples:
# set -g @plugin 'github_username/plugin_name'
# set -g @plugin 'git@github.com/user/plugin'
# set -g @plugin 'git@bitbucket.com/user/plugin'

# Initialize TMUX plugin manager (keep this line at the very bottom of tmux.conf)
run '~/.tmux/plugins/tpm/tpm'

# plugin customization
set -g @online_icon "#[bg=colour236 fg=#00ff00]"
set -g @offline_icon "#[bg=colour236 fg=#ff0000]"

# plugins }

# things to refer

#I -> window no. #P -> panel no.
# Dump tmux colors
# for i in {0..255} ; do
#     printf "\x1b[38;5;${i}mcolour${i}\n"
# done

# Das Ende der Datei
